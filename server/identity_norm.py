"""사용자 신원 키 정규화 — 이 서버가 '같은 사람' 을 판정하는 단일 규칙.

한 사람이 여러 표기로 들어온다:
  ``SECDS\\chumji.kim`` (Windows 도메인 포함) · ``chumji.kim`` · ``Chumji.Kim`` (대소문자).
Windows 계정은 대소문자를 구분하지 않고 도메인은 소속 표기일 뿐이라 셋 다 한 사람이다.
정규화가 빠진 경로가 하나라도 있으면 그 사람의 접속 통계·즐겨찾기·편집 권한이 여러 행으로
갈라지고, 관리자 사용자 현황에 같은 사람이 3~4명으로 나온다 (2026-08-14 통합).

규칙: **마지막 백슬래시 뒤만 취하고 → 공백 제거 → 소문자.**
신원 없는 접속의 ``ip:1.2.3.4`` 표기와 예약 계정(``admin-panel``·``system``)은 백슬래시가
없고 이미 소문자라 규칙을 그대로 통과한다 — 별도 예외가 필요 없다.

이 모듈은 표준 라이브러리조차 쓰지 않는다. 신원 계층(auth_identity)·라우트·관리자 집계·
DB 병합 스크립트가 **같은 판정**을 쓰려면 어디서든 import 될 수 있어야 하기 때문이다.
SQL 쪽 동치 표현은 database/sessions.py 의 ``_UPLOADER_MATCH`` 주석 참조.
"""


def normalize_uid(value):
    """사용자 식별 문자열 → 정규화 키. None·빈 값은 ""."""
    return str(value or "").split("\\")[-1].strip().lower()
