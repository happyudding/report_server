# 활성 서버(xlsx 업로드/조회 + web_report) 실제 의존성만.
flask>=3.0
werkzeug>=3.0
waitress>=3.0
boto3>=1.34
pillow>=10.0
pandas>=2.2
pyarrow>=15.0
numpy>=1.26      # web_report cpk/distribution 계산 (pandas 가 끌어오지만 명시)
psutil>=5.9
pyyaml>=6.0      # eval_analyzer(eval_engine) rules/*.yaml 로딩 — web_report/ai_comment.py 경유

# ── 아래는 _reference/ 분석·시각화 파이프라인 재활성화 시에만 필요 ──
# 현재 활성 서버 코드는 import 하지 않으므로 기본 설치에서 제외.
# dash>=2.17
# kaleido>=0.2.1
# plotly>=5.22
# cairosvg  — SVG→PNG (xlsx thumbnails)
