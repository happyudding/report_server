# 평가 제외 목록 — 여기 걸리는 item 은 어떤 signature 도 발화하지 않고
# 코멘트 저장(L6)도 건너뛴다(AI Comment 미생성). /pe/eval Signatures 탭에서 편집.
# item_contains: item 명(원문) 부분일치, 대소문자 무시
# units: UNIT 원문 정확일치, 대소문자 무시
item_contains: []
units: []
