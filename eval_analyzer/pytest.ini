[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: report_generator(df_honey) + 실제 raw CSV 연계 테스트 (느림, 환경 의존)
    rules_as_deployed: signatures.yaml 의 enabled 플래그를 있는 그대로 본다 (conftest 의
        all_signatures_enabled autouse fixture 해제 — 비활성 메커니즘 자체를 검증할 때만)
