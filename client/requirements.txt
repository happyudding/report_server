# Qt/Chromium 은 == 로 고정한다. 범위(>=)로 두면 빌드 PC 에서 pip 이 최신을 끌어와
# 클라 코드를 한 줄도 안 고친 배포에서 내장 브라우저의 Chromium 이 통째로 바뀐다 —
# 특정 PC 에서만 나는 렌더링 이상(깜빡임·렌더러 크래시)의 원인을 배포와 분리할 수 없게 된다.
# 올릴 때는 의도적으로 올리고 문제 PC 에서 확인할 것.
PyQt6==6.11.0
PyQt6-WebEngine==6.11.0  # 내장 브라우저 (embedded_browser.py) — Chromium(Plotly 렌더)
requests>=2.28
requests-toolbelt>=1.0  # 업로드 진행률(%) 추적 (transport/uploader.py)
pywin32>=306   ; sys_platform == "win32"   # Excel COM 시트/이미지 추출 (report_flow/upload_prepare.py 등)
pandas>=1.5    # 로컬 리포트 분석 엔진 (report_generator)
pyarrow>=15.0  # webreport parquet bytes encoding
numpy>=1.23    # 분석 계산 (cpk/yield/distribution)
matplotlib>=3.5    # map_report (wafer bin map → PNG)
xlwings>=0.30  ; sys_platform == "win32"   # 로컬 xlsx 리포트 생성 (Excel COM)
XlsxWriter>=3.1    # Excel Download 기본 엔진 (excel_download/_xlsx.py) — Excel 없이 xlsx 직접 생성
pystdf         # honey_parse(외부 프로젝트) STDF 파싱 의존성
