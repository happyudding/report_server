Progress Bar 에서 시간이 step 마다 멈춰 있는데 시간은 초단위로 따로 계속 흘러갔으면 좋겠다. 멈춰 있으니까 마치 tool 이 멈춰 있는것 처럼 보인다. 모든 Progress bar 에 그렇게 적용 가능할까?


excel 생성 관련
CPK 가 생성 안되는 상태(option 에서 CPK uncheck 상태)면 Issue Table 에 CPK 부분은 빈칸으로 놔둬줘
Cpk Item명 C열 ISsue_table D열 열너비 2배로 해줘 (Item 명)
Raw data check 했을때 만들어지는 sheet 는 눈금선 추가해줱

Main ui
Product Type, Local File Server upload 버튼 반반 나눠주고 Local File server upload 버튼은 grid 내 버튼 꽉채워줘

client/honey_main.py 수정해줘
배경 : NASCA DRM xlsx 는 ZIP 이 아니라 zipfile.zipFILE() 검증에서 차단됨. xlwings 로 우회해야함.

수정 내용 3가지 
_extract_via_xlwings(src_path)함수 추가
xw.app(visible=False, add_book=False) 로 열어 DRM 자동 복호화
각 시트 cell 값을 used_range.value 로 읽어 openpyxl workbook 에 위치 보존하며 복사(distribution/_dist 시트 제외)
issue_table 시트 이미지는 sheet.pictures -> pic.api.Copy() -> win32clipboard PNG format 으로 추출, row 는 pic.api.Topleftcell.row - (header_row+1)(0-based)
tempfile 에 저장 후 (tmp_path, issue_imgs) 반환, finally 에서 wb.close(); app.quit()
2. _prepare_upload_xlsx() 반환 을 3tuple 로 변경
BadzipFile 시에 헤더에 "NASCA" 있으면 _extract_via_xlwings() 호출, 실패 시 에러
3. _do_upload() 에서 3-tuple 언패킹 후 uploader.post_xlsx(..., issue_imgs = issue_imgs) 추가