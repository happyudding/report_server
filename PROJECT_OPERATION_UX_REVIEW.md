# 전체 프로젝트 운영·정상동작·UX 검토 보고서

> 검토 기준일: 2026-07-15 (Asia/Seoul)  
> 검토 커밋: `0bdd44a48027d6f972ced50434d277fcdec26f5e` (`master`)  
> 검토 범위: 현재 커밋과 작업 트리, 서버·클라이언트·DB·스토리지·문서, 격리 실행 및 브라우저 검증  
> 변경 원칙: 소스·설정·DB·업로드·백업은 수정하지 않고 이 보고서만 추가

## 1. 결론

### 가장 위험한 문제

`POST /api/report/<session_id>/rawdata_replace`는 실제 편집자 권한, CSRF, 원본 버전 보존 없이 `X-Honey-Agent` 헤더 존재 여부만 보고 세션 원본을 덮어쓴다. 격리 실행에서 임의 클라이언트가 헤더 하나만 넣어 원본 데이터를 교체할 수 있음을 확인했다. 교체 전 원본에 대한 서버 측 이력·복원 기능도 없다.

이 문제는 사용자가 수용한 “Honey 사용자 식별 헤더 위조 가능성”과 별개다. 해당 식별 체계를 신뢰한다고 가정해도 이 경로는 사용자 ID·세션 소유자·위임 편집자 권한을 검사하지 않는다.

### 즉시 운영 판단

- **인터넷 또는 다수 사용자가 접근하는 신규 운영 배포: 보류 권고**
- **신뢰된 소수 사용자만 접근하는 격리 운영: 조건부 가능**
- 조건부 운영의 최소 전제는 리버스 프록시에서 원본 교체 API를 차단하거나 극소수 출발지로 제한하고, 업로드 요청 크기·동시 수를 제한하며, 백업을 다른 장치로 복제하는 것이다.

종합 운영 준비도는 **44/100, “제한된 내부 시험 운영 수준”**으로 판정한다. 점수는 표준 인증 점수가 아니라 이번 검토의 위험도·재현 결과·실환경 미검증 범위를 종합한 상대 평가다.

| 영역 | 준비도 | 핵심 판단 |
|---|---:|---|
| 운영 안정성 | 40/100 | 강제 포트 종료, 자동 재시작 부재, 얕은 헬스체크, 로그 크기 회전 부재 |
| 데이터 안정성 | 35/100 | 무이력 원본 덮어쓰기, DB 생성 전 저장소 기록, 동일 디스크 백업, 삭제 잔여 데이터 |
| 보안·권한 | 45/100 | 수용 항목을 제외해도 업로드·원본 교체·주석 API의 권한 경계가 불충분 |
| 정상동작 | 62/100 | 주 사용자 여정은 동작하지만 배포 의존성, XLSX 계수, 업데이트 배포가 실제로 깨짐 |
| 성능·용량 | 38/100 | 2GB 요청, 파일 전체 메모리 적재, 긴 계산 대기, 워커별 대형 캐시 상한 |
| UX | 57/100 | 핵심 탭은 동작하나 404 상태·좁은 화면 표·깨진 다운로드 진입점이 불편 |
| 문서 정합성 | 34/100 | 포트·바인딩·서버 URL·권한 설명이 코드와 다름 |

### 상위 10개 위험

| 순위 | ID | 등급 | 상태 | 요약 |
|---:|---|---|---|---|
| 1 | F-01 | P0 | 재현 확인 | 헤더 하나로 세션 원본을 이력 없이 덮어쓸 수 있음 |
| 2 | F-02 | P1 | 노출 확인/영향 정적 확인 | 무인증 업로드와 2GB 요청·파일 전체 메모리 적재 조합 |
| 3 | F-03 | P1 | 장애 주입 확인 | 파일 저장 후 DB 기록 실패 시 영구 고아 저장소가 남음 |
| 4 | F-05 | P1 | 현재 환경 확인 | 기존 서버 가상환경에 YAML 의존성이 없어 eval export가 조용히 실패 |
| 5 | F-04 | P1 | HTTP 재현 확인 | 대시보드 Honey 다운로드가 404이며 릴리스 해시도 비어 있음 |
| 6 | F-06 | P1 | 구성 확인 | 시작 시 같은 포트의 프로세스를 강제 종료하며 서비스 감시·자동 재시작이 없음 |
| 7 | F-07 | P1 | 경로/백업 확인 | 백업이 원본과 같은 저장소 트리에 있고 복원 절차·사전 마이그레이션 백업이 없음 |
| 8 | R-01 | P1 | 높은 가능성/실부하 미검증 | 긴 계산이 Waitress 스레드를 점유하고 워커·디스크 캐시가 큰 상한을 가짐 |
| 9 | F-09 | P1 | 재현 확인 | 존재하지 않는 세션에 주석 생성, 작성자 확인 없이 수정·삭제 가능 |
| 10 | F-12 | P2 | 브라우저 재현 확인 | 404 상세 화면에 영구 “로드 중”과 활성 저장·삭제 버튼이 남음 |

### 사용자 수용 전제

다음 두 항목은 사용자 지시에 따라 이번 운영 중단 판단, 상위 위험, 준비도 감점에서 제외했다.

1. 기본 관리자 인증 경로·초기 인증값
2. Honey User-Agent 기반 사용자 식별의 위조 가능성

원문 인증값이나 비밀값은 열람·기록하지 않았다. 단, 사용자 식별 위조와 무관하게 아예 권한 검사가 없는 API는 별도 결함으로 평가했다.

---

## 2. 검토 방법과 보존 범위

### 기준선

- 시작 시 `git status --porcelain=v1 -uall`, `git diff --stat`, `git diff --check`는 모두 깨끗했다.
- 검토 시작 시 5000, 8000, 8080 포트에서 프로젝트 서버는 실행 중이지 않았다.
- 운영 후보 DB·업로드·백업의 내용은 열람하지 않고 파일 수·크기·수정 시각 메타데이터만 기록했다.
- 운영 자격증명과 실제 사용자 데이터, 비밀키 본문은 열람하지 않았다.
- 공개 저장소에는 이슈와 현재 커밋의 GitHub Actions 실행 이력이 없어 로컬 코드와 직접 검증 결과를 주 근거로 삼았다.

### 격리 원칙

- 임시 디렉터리에 별도의 SQLite DB, 업로드, eval 경로를 구성했다.
- 스케줄러를 끄고 운영 DB·업로드·백업에 접근하지 않는 앱 인스턴스를 사용했다.
- `PYTHONDONTWRITEBYTECODE=1`, pytest cache 비활성화로 바이트코드·테스트 캐시 생성을 막았다.
- 브라우저 검증은 격리 서버의 합성 세션만 사용했고 종료 후 검증 포트를 닫았다.
- 실제 Excel COM, 실제 S3, 파괴적인 복원 시험, 2GB 요청, 대규모 실사용 데이터 부하는 수행하지 않았다.

### 심각도 기준

| 등급 | 기준 |
|---|---|
| P0 | 데이터 무결성·기밀성·가용성에 즉각적이고 광범위한 손상이 가능해 공개 운영을 막아야 함 |
| P1 | 높은 확률의 장애·데이터 손실·핵심 기능 실패로 단기간 내 조치가 필요함 |
| P2 | 제한된 조건의 기능 오류, 누적 운영 위험, 뚜렷한 UX 저하 |
| P3 | 개선 권고, 방어 심화, 경미한 불편 또는 문서 품질 문제 |

---

## 3. 검증 결과 요약

### 자동·정적 검증

| 검증 | 결과 | 해석 |
|---|---|---|
| 전체 지정 pytest | **90 passed / 0 failed**, 5.49초 | 기존 회귀 테스트는 통과했으나 아래 권한·장애 주입·브라우저 결함은 포함하지 않음 |
| Python AST 구문 검사 | 506개 파일 / 오류 0 | 구문 수준 통과이며 import·실행 성공을 의미하지 않음 |
| 사용자 작성 JavaScript `node --check` | 18개 파일 / 오류 0 | 구문 수준 통과 |
| XLSX 제공 픽스처 직접 실행 | issue 행 3개 입력, `issue_rows=0` | `Bin`/`bin` 대소문자 처리 결함 확인 |
| 서버 `.venv` eval 엔진 import | 실패: `yaml` 모듈 없음 | 현재 가상환경과 requirements 불일치 확인 |
| SQLite 온라인 백업 | 생성·`integrity_check=ok` | 생성은 정상, 실제 복원 절차는 미검증 |

### 격리 API 검증

| 시나리오 | 결과 | 판정 |
|---|---|---|
| `/healthz`, 검색, 버전 API | 200 | 기본 응답 정상 |
| 존재하지 않는 세션 조회 | 404 | API 상태 코드는 정상 |
| web_report 업로드 → 조회 → Raw 편집 → Distribution ETag 갱신 | 모두 성공 | 핵심 계산·캐시 무효화 흐름 정상 |
| 소유자 → 편집자 위임 → 비공개 → 편집자 접근 | 모두 성공 | 일반 권한 여정 정상 |
| 비공개 후 익명 상세 조회 | 404 | 비공개 조회 차단 정상 |
| 세션 삭제 후 조회 | 404 | 화면상 세션 삭제 정상, 관련 테이블 잔여는 F-08 참조 |
| Honey 헤더만 넣은 Raw Data 교체 | 200 | 권한 우회 및 원본 덮어쓰기 F-01 확인 |
| Honey 헤더 없는 xlsx/web_report 업로드 | 200 | 무인증 업로드 F-02 확인 |
| 빈 필수 메타데이터·잘못된 mode 업로드 | 200 | 검증 누락 F-10 확인 |
| 없는 세션에 주석 생성 | 201 | 참조 무결성 결함 F-09 확인 |
| 익명 주석 수정·삭제 | 200/200 | 주석 소유권 검사 누락 F-09 확인 |
| Honey 버전 조회/다운로드 | 200/404 | 릴리스 파일 부재 F-04 확인 |
| 공개 thread dump API | 200 | 내부 스택 정보 노출 F-15 확인 |

### 주요 사용자 여정

| 여정 | 상태 | 비고 |
|---|---|---|
| web_report 업로드·분석 | 정상 | 합성 40 item/200 die 데이터로 확인 |
| XLSX 업로드·yield 계산 | 부분 정상 | yield는 생성됐으나 Issue Rows 계수가 0으로 잘못됨 |
| 검색·검색 결과 없음 | 정상 | 빈 결과 메시지가 명확함 |
| 상세 Summary/Yield/CPK/Issue/Distribution/Trim/Map/Note 탭 | 정상 | 1440×900, 1024×768에서 모두 전환·렌더링 확인 |
| Raw Data 편집 후 계산 갱신 | 정상 | 값 변경과 Distribution ETag 변경 확인 |
| 편집자 위임·비공개 접근 | 정상 | 위임 편집자는 접근, 익명은 차단 |
| 존재하지 않는 상세 세션 | 실패 | 오류문과 로딩·활성 작업 버튼이 동시에 남음 |
| Honey 다운로드 | 실패 | 대시보드 링크가 404 |
| 1024×768 검색 목록 식별 | 불편 | Product/Process/LOT 등이 복구 불가능하게 말줄임 |
| Excel COM 열기·내보내기 | 미검증 | 실제 Windows Excel 세션을 사용하지 않음 |
| 실제 S3 저장·복구 | 미검증 | 자격증명·실데이터를 사용하지 않음 |

### 합성 동시 요청 스모크

10명, 12초, 40 item/200 die 합성 세션으로 실행했다. 위험을 줄이기 위해 계산 ProcessPool은 끈 상태이므로 운영 부하 성능을 대표하지 않는다.

| 요청 | 건수 | p50 | p95 | 최대 | 오류 |
|---|---:|---:|---:|---:|---:|
| session full | 68 | 0.04s | 0.18s | 0.24s | 0 |
| cold full | 20 | 0.10s | 0.21s | 0.21s | 0 |
| upload | 20 | 0.15s | 0.41s | 0.41s | 0 |
| distribution | 25 | 0.02s | 0.07s | 0.08s | 0 |
| index | 46 | 0.00s | 0.02s | 0.15s | 0 |
| healthz | 46 | 0.01s | 0.04s | 0.05s | 0 |

관측 최고 CPU는 약 106%, 평균 45%, RSS는 약 0.13GB였다. 이는 작은 합성 데이터의 정상 경로 확인일 뿐, 대규모 parquet·2GB 업로드·ProcessPool·실제 S3에 대한 용량 근거가 아니다.

---

## 4. 확인된 결함

### F-01 [P0] Raw Data 원본을 권한·CSRF·버전 없이 덮어씀

- **확신도/영역:** 높음 / 데이터 무결성, 권한, 정상동작
- **근거:** `server/report/routes_webreport.py:375-397`은 `X-Honey-Agent` 존재만 확인하고 `_editor_guard`, `_require_csrf`를 호출하지 않는다. `web_report/rawedit.py:42-99`는 원본 파일과 `content_hash`를 교체하며 이전 버전·undo를 만들지 않는다.
- **발생 조건:** 서버에 접근 가능한 클라이언트가 대상 session ID를 알고 임의 헤더를 붙여 교체 파일을 POST.
- **영향:** 원본 분석 데이터와 이후 계산 결과가 영구 변경된다. 실수와 악의적 교체를 구분·복구하기 어렵다.
- **재현/확인:** 격리 세션에 사용자 ID·CSRF 없이 헤더만 넣어 호출했으며 HTTP 200과 `ok` 응답을 확인했다.
- **임시 완화:** 리버스 프록시에서 해당 경로를 차단하거나 관리 단말의 고정 출발지만 허용한다. 교체 전 업로드 디렉터리를 별도 매체에 복제한다.
- **권장 수정:** 일반 편집 API와 동일한 세션 편집자 검증·CSRF를 적용하고, 원본을 불변 버전으로 보관한 뒤 감사 로그와 복구 API를 제공한다.

### F-02 [P1] 업로드가 무인증이며 대용량 요청을 메모리에 중복 적재할 수 있음

- **확신도/영역:** 노출 높음, 장애 규모 중상 / 가용성, 성능, 비용
- **근거:** `server/upload_webreport.py:80-105`, `server/upload_xlsx.py:154-310`에 사용자·Honey 헤더·CSRF 검증이 없다. `server/upload_webreport.py:41-57`은 파일을 통째로 읽고 파일당 최대 512MB를 허용하며 파일 수 상한이 없다. `server/wsgi.py:127-129`의 전체 요청 상한은 2GB, `server/wsgi.py:174`의 Waitress 스레드는 8개다.
- **발생 조건:** 접근 가능한 사용자가 다수 대형 파일을 동시에 전송하거나 반복 업로드.
- **영향:** 요청 바이트, parquet 해석용 pandas 객체, 계산 입력이 겹쳐 RAM 고갈·GC 지연·전체 응답 정지가 가능하다.
- **재현/확인:** 헤더 없이 xlsx와 web_report 업로드가 모두 200임을 확인했다. 자원 보호를 위해 512MB~2GB 실요청은 실행하지 않았다.
- **임시 완화:** 프록시에서 업로드 경로를 IP 제한하고 body 크기, 요청 속도, 동시 업로드 수를 낮게 제한한다. 서버 메모리·swap·OOM 이벤트를 경보한다.
- **권장 수정:** 인증/권한, 파일 수·합계 크기, 스트리밍 저장, 업로드 admission queue, 타입·구조 선검증을 추가한다.

### F-03 [P1] 파일 저장 성공 후 DB 실패 시 정리할 수 없는 고아 데이터가 남음

- **확신도/영역:** 높음 / 데이터 수명주기, 디스크 용량
- **근거:** `web_report/ingest.py`는 저장소·캐시 기록 후 세션 DB 행을 생성한다. `server/report_cleanup.py:100-129` 정리는 DB에 존재하는 pending/orphan 세션만 순회하므로 세션 행이 만들어지기 전 실패한 디렉터리는 발견하지 못한다.
- **발생 조건:** 원본 저장 후 DB lock, 디스크 오류, 프로세스 종료, 예외 발생.
- **영향:** 화면에서 보이지 않는 parquet·manifest가 영구 누적되고 저장 공간을 소모한다.
- **재현/확인:** 격리 저장소에서 DB write 실패를 주입했다. 세션 행 없이 `manifest.json`, `source_0.parquet`가 남았으며 현 cleanup 경로로는 식별되지 않았다.
- **임시 완화:** 업로드 저장소와 DB session/analysis_key를 주기적으로 대조하고 불일치는 삭제 전에 격리 목록으로 보관한다.
- **권장 수정:** DB 예약 행을 먼저 만들거나, 실패 시 로컬·S3·캐시를 보상 삭제하는 트랜잭션성 ingest를 구현한다.

### F-04 [P1] Honey 다운로드·업데이트 배포물이 실제로 없음

- **확신도/영역:** 높음 / 배포, 정상동작, UX
- **근거:** `server/releases/version.json:2-5`는 ZIP 파일명을 가리키지만 해시가 비어 있고, releases 디렉터리에는 `version.json`만 있다. `server/honey_routes.py:35-43`은 파일 부재 시 404를 반환한다. `client/transport/version_check.py:42-80`은 해시가 비면 무결성 검증을 생략한다.
- **발생 조건:** 대시보드에서 Honey 다운로드 클릭, 또는 구버전 클라이언트가 업데이트 파일 요청.
- **영향:** 신규 설치가 막히고 구버전 자동 업데이트가 실패한다. 향후 파일만 올리고 해시를 비우면 전송 무결성도 확인하지 않는다.
- **재현/확인:** 버전 API는 200, 다운로드 API는 404였다. 1440×900 대시보드의 눈에 띄는 다운로드 버튼도 같은 실패 경로를 가리켰다.
- **임시 완화:** 검증된 ZIP과 SHA-256을 함께 게시하기 전에는 다운로드 진입점을 운영 안내에서 제외하고 별도 검증 채널을 제공한다.
- **권장 수정:** 릴리스 생성·해시·배치·다운로드 smoke test를 하나의 원자적 배포 단계와 CI 체크로 묶는다.

### F-05 [P1] 현재 서버 가상환경이 requirements와 달라 eval export가 실패함

- **확신도/영역:** 높음 / 배포 재현성, 정상동작
- **근거:** `server/requirements.txt`에는 PyYAML이 있으나 현재 `server/.venv`에는 `yaml`이 없다. `server/start.bat:29-47`은 `.venv`가 이미 존재하면 requirements를 다시 설치·동기화하지 않는다. `server/install.bat`의 import 확인 목록에도 YAML이 없다.
- **발생 조건:** 현재 `.venv`로 eval export 엔진을 로드하거나, requirements가 바뀐 뒤 기존 환경으로 재시작.
- **영향:** 업로드 본문은 성공해도 best-effort 후처리인 eval export가 경고만 남기고 누락되어 사용자는 성공으로 오인할 수 있다.
- **재현/확인:** 현재 `.venv` Python으로 eval 엔진을 import해 `ModuleNotFoundError: No module named 'yaml'`을 확인했다.
- **임시 완화:** 유지보수 창에서 승인된 설치 절차로 의존성을 동기화하고 eval export를 별도 smoke test한다.
- **권장 수정:** 버전 고정 lock, 설치 상태 fingerprint, 시작 전 필수 import/기능 검사, 실패 시 readiness 비정상 및 사용자-visible 상태를 제공한다.

### F-06 [P1] 시작·종료가 강제적이고 프로세스 감시가 없음

- **확신도/영역:** 높음 / 운영 안정성
- **근거:** `server/start.bat:5-8`은 기본 8080과 `0.0.0.0`을 강제하고 먼저 `terminate.bat`을 실행한다. `server/terminate.bat:4-11`은 해당 포트 listener PID를 `Stop-Process -Force`로 종료한다. 서비스 관리자·자동 재시작·graceful shutdown 구성은 없다.
- **발생 조건:** 재배포, 포트 충돌, 프로세스 crash, Windows 재부팅.
- **영향:** 다른 애플리케이션을 잘못 종료할 수 있고 처리 중 요청·SQLite write가 중단될 수 있다. crash 후 사람 개입 전까지 서비스가 계속 중단된다.
- **재현/확인:** 스크립트 정적 검토로 확정했다. 운영 프로세스에 대한 강제 종료 시험은 하지 않았다.
- **임시 완화:** 실행 전 포트 PID의 실행 경로를 확인하고, 서비스 래퍼에서 정상 종료 시간과 재시작 정책을 둔다.
- **권장 수정:** Windows Service 또는 동등한 supervisor로 단일 인스턴스·graceful stop·restart backoff·부팅 자동 시작을 관리한다.

### F-07 [P1] 백업은 생성되지만 동일 장애 영역이며 복원 운영이 정의되지 않음

- **확신도/영역:** 중상 / 재해복구, 데이터 안정성
- **근거:** `server/db_backup.py:36-69`은 SQLite online backup과 integrity check를 수행하지만 기본 백업 경로는 DB와 같은 프로젝트 저장소 트리다. 실제 메타데이터도 같은 저장 장치에 DB 1개, 백업 7개가 있음을 보였다. `server/database/core.py:376-384`는 시작 시 자동 migration을 백업보다 먼저 수행하며 migration ledger·사전 백업 절차가 없다.
- **발생 조건:** 디스크 손상·랜섬웨어·프로젝트 트리 삭제, 잘못된 migration, 복원 긴급 상황.
- **영향:** 원본과 백업이 함께 손실되거나, 어떤 백업을 어떤 순서로 복원할지 몰라 RTO/RPO가 예측 불가능하다. integrity 실패한 새 백업도 남은 채 오래된 정상 백업이 rotation될 여지가 있다.
- **재현/확인:** 격리 DB online backup과 `integrity_check=ok`는 확인했다. 실제 운영 복원·애플리케이션 재기동은 파괴 위험 때문에 미실행했다.
- **임시 완화:** 백업을 다른 물리/클라우드 저장소로 복제하고 날짜별 복구 사본을 보존한다. 수동 복원 runbook과 담당자를 지정한다.
- **권장 수정:** 시작 migration 전 백업, schema version ledger, 검증 실패 백업의 rotation 제외, 정기 자동 복원 시험과 RPO/RTO 경보를 도입한다.

### F-08 [P2] 세션 삭제 후 사용자 관계 데이터가 남음

- **확신도/영역:** 높음 / 데이터 수명주기, UX
- **근거:** `server/database/sessions.py:45-50` 삭제는 annotation, webreport edit/revision, session 중심이며 `session_editor`, `user_favorite`, `user_important`를 지우지 않는다. `server/database/core.py:178-209` 관련 테이블에는 강제하는 외래키가 없다.
- **발생 조건:** 편집자·즐겨찾기·중요 표시가 있는 세션 삭제.
- **영향:** 고아 행이 누적되고, 같은 ID 재사용·관리 통계·사용자 목록에서 예기치 않은 상태를 만들 수 있다.
- **재현/확인:** 격리 세션 삭제 후 session 0건인데 editor/favorite/important가 각각 1건 남음을 확인했다.
- **임시 완화:** 읽기 전 session 존재를 항상 확인하고 주기적으로 관계 테이블의 고아 행을 보고한다.
- **권장 수정:** 하나의 DB transaction에서 모든 관계 행을 삭제하거나 외래키 `ON DELETE CASCADE`를 활성화한다.

### F-09 [P1] 주석의 세션·작성자 무결성이 보장되지 않음

- **확신도/영역:** 높음 / 데이터 무결성, 권한
- **근거:** `server/report/routes_misc.py:31-70`은 생성 시 session 존재를 확인하지 않고, 수정·삭제는 annotation ID와 CSRF만 확인하며 작성자·세션 편집 권한을 검사하지 않는다.
- **발생 조건:** 임의 session ID로 생성하거나 다른 주석 ID를 알아내 수정·삭제.
- **영향:** 유령 주석과 타 사용자 주석 변조·삭제가 가능해 협업 기록의 신뢰성이 무너진다.
- **재현/확인:** 존재하지 않는 세션에 생성 201, 신원 없이 CSRF 쿠키만 사용한 수정/삭제 200을 확인했다.
- **임시 완화:** 프록시에서 주석 쓰기 API를 인증된 내부 경로로 제한하고 주석 변경 로그를 보존한다.
- **권장 수정:** session FK·존재 검사, 작성자 또는 세션 편집자 권한, CSRF를 함께 적용한다.

### F-10 [P2] web_report 필수 메타데이터와 mode가 엄격히 검증되지 않음

- **확신도/영역:** 높음 / 데이터 품질, 검색 UX
- **근거:** `web_report/validation.py:64-87`은 문자열을 trim/truncate할 뿐 필수값을 거부하지 않고, `web_report/validation.py:54-58`은 알 수 없는 mode를 Normal로 바꾼다.
- **발생 조건:** 빈 product/lot 등 또는 오타 mode 업로드.
- **영향:** 검색·필터에 식별 불가능한 세션이 생기고 잘못된 분석 mode가 정상 데이터처럼 저장된다.
- **재현/확인:** 빈 메타데이터와 invalid mode 업로드가 200 `done`으로 완료됨을 확인했다.
- **임시 완화:** 업로더에서 필수값·허용 mode를 사전 검사하고 운영자가 불완전 세션을 정기 조회한다.
- **권장 수정:** 서버를 정본으로 required/enum/schema를 검증하고 4xx 필드별 오류를 반환한다.

### F-11 [P2] XLSX Issue Rows 계수가 헤더 대소문자 때문에 0이 됨

- **확신도/영역:** 높음 / 정상동작, 데이터 표시
- **근거:** `server/xlsx_parser.py:107-119`는 `r.get("bin")`을 찾지만 `server/xlsx_parser.py:422-433`은 원래 헤더 대소문자를 유지한다. 제공 픽스처 `tests/sample_xlsx.py:69-85`는 `Bin`을 사용한다.
- **발생 조건:** Issue 표 헤더가 `Bin` 또는 다른 대소문자로 들어옴.
- **영향:** 업로드 응답·legacy 계수에서 실제 issue가 없는 것처럼 보인다. `sheet_data.issue_table` 자체는 남으므로 모든 Issue UI가 사라진다고 단정할 수는 없다.
- **재현/확인:** issue 행 3개가 든 제공 픽스처 실행 결과 `issue_rows=0`을 확인했다.
- **임시 완화:** 운영 확인 시 응답 계수만 믿지 말고 실제 issue table을 대조한다.
- **권장 수정:** 헤더 canonicalization과 대소문자·공백 변형 회귀 테스트를 추가한다.

### F-12 [P2] 상세 404 화면이 영구 로딩과 활성 작업 버튼을 남김

- **확신도/영역:** 높음 / UX, 오류 처리
- **근거:** `server/report/static/webreport/boot.js:53-74`는 fetch 실패 시 오류문만 표시하고 조기 return한다. 버튼 상태 처리(`boot.js:110-114`)와 본문 placeholder 교체까지 도달하지 않는다. 기본 placeholder와 작업 버튼은 `server/report/report_view.html:1628-1634,1686-1695`에 있다.
- **발생 조건:** 삭제됐거나 존재하지 않는 `/view/<session_id>` 직접 접근.
- **영향:** 사용자는 “세션을 불러올 수 없습니다 (404)”와 “로드 중...”을 동시에 보고, 저장·즐겨찾기·비공개·삭제 버튼을 누를 수 있어 상태를 오해한다.
- **재현/확인:** 1440×900 브라우저에서 오류, 영구 로딩, `disabled=false`인 작업 버튼을 확인했다.
- **임시 완화:** 사용자 안내에 목록으로 돌아가 재검색하도록 명시한다.
- **권장 수정:** 공통 fatal error state에서 로딩 placeholder와 탭을 제거하고 모든 mutation 버튼을 숨기거나 비활성화한다.

### F-13 [P2] 1024×768 목록에서 핵심 식별값이 복구 불가능하게 잘림

- **확신도/영역:** 높음 / 반응형 UX
- **근거:** `server/report/report_analysis_index.html:160-165`는 fixed-layout과 전 셀 ellipsis를 적용하며 대응 media query가 없다. 렌더링에서 filename/uploader에는 title이 있지만 product/LOT 등에는 없다(`:867-868` 주변).
- **발생 조건:** 1024px 폭 또는 작은 노트북/분할 창.
- **영향:** Product Type, mode, product, process, LOT, uploader/date가 동시에 잘리고 product/LOT은 hover로도 전체값을 알 수 없어 올바른 세션 선택이 어렵다.
- **재현/확인:** 1024×768에서 수평 스크롤 없이 다수 열이 강한 말줄임 처리되고 title도 없음을 브라우저에서 확인했다.
- **임시 완화:** 브라우저 폭을 넓히거나 검색어로 후보를 줄인다.
- **권장 수정:** 작은 화면에서 우선 열만 표시하고 상세 행/카드로 확장하거나 핵심 셀 tooltip·가로 스크롤을 제공한다.

### F-14 [P2] 실행 문서와 실제 기본값·권한 동작이 다름

- **확신도/영역:** 높음 / 운영 문서
- **근거:** 아래 문서 정합성 표 참조.
- **발생 조건:** 신규 설치, 장애 대응, 클라이언트 배포, API 연동.
- **영향:** 잘못된 포트·바인딩·서버 URL로 연결 실패하거나, 문서상 존재하는 보호가 실제에도 있다고 오판한다.
- **재현/확인:** 코드와 README를 직접 대조했다.
- **임시 완화:** 운영 runbook에 현재 실제 포트·바인딩·URL·권한 제약을 별도 고정한다.
- **권장 수정:** 설정을 단일 정본으로 만들고 README 예제를 시작 smoke test와 함께 검증한다.

### F-15 [P2] 공개 thread dump API가 내부 스택을 반환함

- **확신도/영역:** 높음 / 정보 노출, 운영 진단
- **근거:** `server/report/routes_misc.py:266-277`은 인증 없이 모든 thread stack을 반환한다.
- **발생 조건:** 서버 접근자가 진단 API 호출.
- **영향:** 내부 함수명·파일 경로·처리 중 위치가 노출되고 큰 응답 생성 자체가 장애 분석 중 부하를 더할 수 있다.
- **재현/확인:** 격리 서버에서 익명 요청 200을 확인했다.
- **임시 완화:** 프록시에서 진단 경로를 관리망으로 제한한다.
- **권장 수정:** 관리자 권한과 감사 로그를 적용하고 운영 빌드에서는 기본 비활성화한다.

### F-16 [P2] 기본 응답 보안 헤더가 부족함

- **확신도/영역:** 높음 / 웹 방어 심화
- **근거:** 앱 전역 CSP, HSTS, `X-Content-Type-Options`, Referrer/Permissions Policy 설정을 찾지 못했다. 격리 index 응답에서 CSP와 HSTS가 없음을 확인했다.
- **발생 조건:** 브라우저 기반 공격, 외부 콘텐츠 삽입, 잘못된 MIME 처리, HTTP 운영.
- **영향:** 다른 결함이 생겼을 때 브라우저 방어 계층이 약하다. HSTS는 HTTPS 전환 후 의미가 있다.
- **재현/확인:** 실제 응답 헤더와 코드 검색으로 확인했다.
- **임시 완화:** HTTPS 프록시에서 안전한 공통 헤더를 먼저 적용하되 현재 inline script/style 호환성을 사전 검사한다.
- **권장 수정:** nonce/hash 기반 CSP를 단계적으로 도입하고 보안 헤더 회귀 테스트를 둔다.

---

## 5. 높은 가능성의 운영 위험

다음은 코드 구성상 위험이 높지만 실제 운영 데이터·S3·최대 부하로 끝까지 재현하지 않은 항목이다.

### R-01 [P1] 콜드 계산이 요청 스레드를 장시간 점유하고 캐시 상한이 큼

- **확신도/영역:** 중상 / 성능, 가용성
- **근거:** `web_report/compute.py:28-32,66-86`은 기본 2 process, child 32 task, 최대 300초 동안 future 결과를 동기 대기하며 timeout이 실행 중 작업을 취소하지 않는다. Waitress는 8 threads다. `web_report/cache.py:27-48`의 메모리 캐시는 프로세스별 최대 수 GB이고 `web_report/disk_cache.py:27-28` 기본 상한은 500GB다.
- **발생 조건:** 캐시가 비어 있는 대형 분석을 8개 이상 동시에 조회.
- **영향:** 모든 HTTP thread가 계산 대기로 막히고 health·검색도 지연될 수 있다. 부모 프로세스와 자식별 데이터 복제로 RAM이 급증할 수 있다.
- **확인 방법:** 운영과 같은 worker 설정·실데이터 사본으로 cold/warm, 1/4/8/16 동시 사용자 부하를 수행하고 RAM·queue·timeout·child recycle을 계측한다.
- **임시 완화:** 대형 계산 동시 수를 프록시/애플리케이션 외부에서 제한하고 cold 요청 시간·RSS·cache 용량을 경보한다.
- **권장 수정:** 계산 job queue와 202/poll 모델, 취소 가능한 timeout, 용량 기반 캐시·전역 메모리 예산을 도입한다.

### R-02 [P2] 업로드마다 prewarm thread를 먼저 만들고 내부에서만 동시 수를 제한함

- **확신도/영역:** 중상 / 자원 고갈
- **근거:** `web_report/compute.py:123-145`는 업로드마다 thread를 생성한 뒤 thread 내부 semaphore에서 기다린다.
- **발생 조건:** 짧은 시간에 많은 업로드.
- **영향:** 실제 계산 동시 수는 제한돼도 대기 thread와 컨텍스트가 계속 늘어날 수 있다.
- **확인 방법:** 수백 개 작은 업로드에서 thread count와 종료 시간을 측정한다.
- **임시 완화:** 외부 업로드 rate limit을 둔다.
- **권장 수정:** 고정 크기 executor/queue에 prewarm job을 넣고 backlog 상한과 중복 제거를 둔다.

### R-03 [P2] S3 부분 업로드 실패가 고아 객체를 만들 수 있음

- **확신도/영역:** 중간 / 저장소 비용, 일관성
- **근거:** `server/storage_gateway/__init__.py:165-234`는 여러 S3 원본 업로드 중 예외가 나면 로컬 fallback으로 전환한다. 이미 올라간 일부 S3 객체의 보상 삭제 흐름은 보이지 않는다.
- **발생 조건:** 다중 파일 S3 업로드 중 일부 성공 후 네트워크/권한 오류.
- **영향:** DB·manifest에서 참조하지 않는 S3 객체가 누적되고 비용·정리 복잡도가 증가한다.
- **확인 방법:** 전용 테스트 bucket에서 N번째 put 실패를 주입하고 객체 목록·DB object_info·manifest를 대조한다.
- **임시 완화:** bucket lifecycle과 inventory 기반 미참조 객체 보고를 둔다.
- **권장 수정:** staging prefix에 올린 뒤 manifest commit, 실패 시 prefix 일괄 삭제, 주기적 reconciliation을 구현한다.

### R-04 [P2] SQLite 5초 write 대기와 자동 migration이 운영 동시성에 취약함

- **확신도/영역:** 중간 / DB 가용성
- **근거:** `server/database/core.py:376-399`은 WAL, synchronous NORMAL, busy timeout 5초를 사용하고 시작 시 별도 schema ledger 없이 자동 migration한다.
- **발생 조건:** 업로드·편집·주석·정리·백업이 동시에 write하거나 migration 중 재시작.
- **영향:** `database is locked` 오류, 부분 운영 중단, migration 상태 판별 어려움이 가능하다.
- **확인 방법:** 운영 수준 write 동시성과 backup/cleanup을 함께 실행하고 lock wait·실패율을 기록한다.
- **임시 완화:** write 작업 시간을 분리하고 DB lock 오류를 경보한다.
- **권장 수정:** 명시적 migration version·단일 writer 전략·재시도 정책을 두고 규모 증가 시 서버형 DB를 평가한다.

### R-05 [P1] 기본 클라이언트 통신과 업데이트가 평문 HTTP이며 업데이트 rollback이 없음

- **확신도/영역:** 중상 / 데이터 전송, 배포 안정성
- **근거:** `client/transport/config.py:8-12`의 기본 서버 URL은 외부 HTTP 주소이며 read timeout은 300초다. `client/transport/updater.py:117-136`은 파일 복사를 단계적으로 수행하고 두 번째 복사 실패 시 전체 rollback이 없다.
- **발생 조건:** 신뢰할 수 없는 네트워크, 중간자 변조, 업데이트 복사 중 권한·디스크 오류.
- **영향:** 업로드 데이터·세션 응답·업데이트 파일이 전송 중 노출/변조될 수 있고 클라이언트 설치가 반쯤 갱신된 상태로 남을 수 있다.
- **확인 방법:** 실제 배포 URL의 TLS·리다이렉트·인증서와 업데이트 실패 주입을 별도 검증한다.
- **임시 완화:** 클라이언트가 HTTPS reverse proxy만 사용하도록 배포 설정을 고정하고, 검증된 ZIP을 수동 배포한다.
- **권장 수정:** HTTPS 강제, 비어 있지 않은 hash/signature 검증, staging 설치 후 atomic swap과 rollback을 도입한다.

### R-06 [P2] 로그·로컬 저장소·디스크 캐시가 장기 운영에서 디스크를 압박할 수 있음

- **확신도/영역:** 중상 / 용량, 장애 복구
- **근거:** `server/wsgi.py:52-80` 로그 정리는 시작 시 파일 수·보관일만 보고 실행 중 파일 크기 회전은 하지 않는다. `web_report/disk_cache.py:103-137`은 상한 점검 시 전체 캐시 파일을 스캔한다. S3가 없고 tiering dry-run이면 원본은 로컬에 계속 남는다.
- **발생 조건:** 장기 무재시작, 오류 로그 폭증, 대형 분석·업로드 누적.
- **영향:** 디스크 full로 DB write·업로드·백업이 동시에 실패하고, 캐시 전체 스캔이 I/O를 추가한다.
- **확인 방법:** 일별 증가량과 free-space, 단일 로그 크기, eviction 소요 시간을 계측한다.
- **임시 완화:** OS/프록시 로그 회전, 저장소별 quota와 20/10% free-space 경보를 둔다.
- **권장 수정:** size+time rotation, 저장소별 독립 볼륨·상한, 증분 cache index, 실행 가능한 cleanup/tiering 정책을 마련한다.

---

## 6. UX 상세 평가

### 잘 동작한 부분

- 검색 결과 없음은 “검색 결과가 없습니다.”로 명확히 표시됐다.
- 합성 세션에서 Summary, Yield, CPK, Issue Table, Distribution, Trim, Map, Note 탭이 두 해상도에서 모두 열렸다.
- Raw 편집 후 값과 Distribution ETag가 갱신돼 캐시 무효화가 동작했다.
- 비공개 세션은 익명에게 404, 위임 편집자에게 정상 노출됐다.
- readonly 상태가 목록과 Note 배너에 표시됐다.

### 주요 불편

1. 대시보드의 Honey 다운로드가 실제 404라 첫 설치 진입점이 막혀 있다(F-04).
2. 없는 상세 페이지에서 오류·로딩·작업 버튼이 동시에 보여 상태가 모순된다(F-12).
3. 1024×768 목록은 세션 식별에 필요한 product/LOT까지 복구 불가능하게 잘린다(F-13).
4. Trim 빈 상태에서도 Excel Download가 활성 상태라 눌렀을 때 무엇이 내려오는지 예측하기 어렵다(P3).
5. `client/honey_main.py:1212-1252`의 파일 로딩은 60초가 지나면 라벨만 바뀌고 취소·명시적 timeout이 없다. `client/transport/config.py:12`의 최대 300초 네트워크 대기에도 사용자가 취소할 수 있는 흐름이 명확하지 않다(P2, 실제 장시간 파일 미검증).
6. Excel COM 오류 메시지와 진행 상태 처리 코드는 존재하지만 실제 Excel 버전·보호 보기·파일 잠금·COM 종료 상황은 검증하지 못했다.

### 화면 크기별 판정

| 화면 | 목록 | 상세 |
|---|---|---|
| 1440×900 | 전반 사용 가능, 일부 긴 값 말줄임 | 모든 탭 사용 가능, 초기 Summary 합성 데이터 기준 약 2.8초 |
| 1024×768 | 핵심 열 다수 과도한 말줄임, product/LOT tooltip 없음 | 탭은 들어가지만 헤더가 감기며 정보 밀도가 높음 |

---

## 7. 문서와 코드의 불일치

| 항목 | 문서 | 실제 코드/동작 | 위험 |
|---|---|---|---|
| 서버 포트 | `README.md:11,30,44-45`, `server/README.md:24-36`은 8000 | `server/start.bat:5`는 8080 | 설치·헬스체크 대상 오류 |
| 바인딩 | server README는 127.0.0.1 예시 | `server/start.bat:5-8`, `server/wsgi.py:146-150`은 기본/강제 `0.0.0.0` | 의도하지 않은 네트워크 노출 |
| 클라이언트 URL | `client/README.md:31`은 localhost:8000 | `client/transport/config.py:8`은 외부 HTTP:8080 | 배포 환경 오접속·평문 통신 |
| Honey 전용 업로드 | `server/README.md:107-108`은 헤더 필수 설명 | 업로더는 헤더를 보내지 않고 서버 upload route도 요구하지 않음 | 보호된 API라는 잘못된 전제 |
| Raw Data 교체 | `server/README.md:143,161`은 CSRF+editor guard 설명 | 실제 route는 Honey 헤더 존재만 확인 | P0 무결성 위험 은폐 |
| 업로드 PIN | `client/transport/uploader.py:18` 문서는 PIN이 필요하다고 설명 | 세션 verify 흐름은 PIN 보호가 적용되지 않는 동작 | 사용자·운영자 기대 불일치 |
| 의존성 설치 | 시작 스크립트만 실행하면 최신 요구사항을 쓴다는 기대 | 기존 `.venv`는 동기화하지 않음 | 환경 drift와 기능 누락 |

문서는 현재 동작을 정확히 적는 것에 그치지 말고, 코드와 문서 중 의도한 정책이 무엇인지 먼저 결정해야 한다. 특히 Raw Data 교체는 문서를 코드에 맞추는 것이 아니라 코드가 문서의 보호 약속을 충족하도록 해야 한다.

---

## 8. 실환경 미검증 가설과 한계

다음은 추측을 사실처럼 판정하지 않았다.

| 항목 | 완료 범위 | 남은 검증 |
|---|---|---|
| Excel COM | 정적 오류·진행 상태 흐름 확인 | 실제 Excel 설치본, 보호 보기, 잠긴 파일, COM process 잔류, 장시간 로딩 |
| 실제 S3 | 저장·fallback 코드 추적 | 부분 실패, 재시도, 권한 만료, 복원, lifecycle, 비용 |
| 운영 ProcessPool | 설정·timeout·캐시 구조 검토 | 실데이터 cold/warm 부하, child recycle, 메모리 복제, 작업 취소 |
| 2GB/대형 parquet | 상한·전체 read 경로 정적 확인 | 실제 peak RSS, temp 디스크, OOM 후 복구 |
| 백업 복원 | 온라인 backup 생성과 integrity 확인 | 별도 디렉터리 복원, 앱 기동, 세션/파일 정합성, RTO/RPO |
| 실제 네트워크 | 기본 HTTP URL과 로컬 응답 확인 | 배포 도메인 TLS, 프록시 body/timeouts, 외부 접근 범위 |
| 장기 cleanup/tiering | 스케줄·조건 정적 검토 | 수개월 데이터, free-space, S3 전환, 고아 reconciliation |

---

## 9. 조치 우선순위

### 즉시 — 공개 운영 전

1. 프록시에서 Raw Data 교체 API를 차단하거나 관리 단말로 제한한다.
2. xlsx/web_report 업로드를 신뢰 구간으로 제한하고 body·rate·동시 요청 상한을 둔다.
3. 운영 `.venv` 의존성을 승인된 절차로 동기화하고 YAML import 및 eval export smoke test를 통과시킨다.
4. 검증된 Honey ZIP과 SHA-256을 배치하거나 다운로드·업데이트 안내를 임시 중지한다.
5. DB 백업을 다른 장애 영역으로 복제하고 수동 복원 runbook을 작성한다.
6. 포트 기반 강제 종료 대신 실행 파일을 확인한 정상 종료와 서비스 감시·자동 재시작을 적용한다.
7. HTTPS endpoint를 클라이언트 정본으로 지정하고 평문 기본 URL 사용을 중단한다.
8. RAM, process/thread 수, DB lock, 디스크 free-space, 5xx, 업로드 실패를 운영 경보로 연결한다.

### 7일 이내

1. F-01에 세션 편집자 권한·CSRF·원본 버전·감사 로그·복구 경로를 구현한다.
2. ingest를 예약 행 또는 보상 삭제 구조로 바꾸고 기존 저장소-DB reconciliation 도구를 만든다.
3. 주석 session/작성자 권한과 세션 삭제 cascade를 고친다.
4. 404 상세 상태를 단일 fatal 화면으로 만들고 mutation 버튼을 제거한다.
5. web_report metadata schema와 XLSX 헤더 canonicalization 회귀 테스트를 추가한다.
6. requirements를 고정하고 신규/기존 venv 모두 같은 설치 검증을 거치게 한다.
7. health와 readiness를 분리해 DB write, 저장소, worker, 릴리스, 필수 의존성을 점검한다.

### 30일 이내

1. 운영과 같은 실데이터 사본으로 1/4/8/16 사용자 cold/warm 부하와 장애 회복 시험을 한다.
2. 계산 job queue, 취소 가능한 timeout, 전역 메모리 예산과 캐시 용량 정책을 설계한다.
3. S3 staging/commit/rollback과 로컬·S3·DB reconciliation을 자동화한다.
4. migration ledger, 사전 backup, 정기 자동 복원 훈련을 구축한다.
5. 1024px 이하 목록을 핵심 열+확장 상세 구조로 개선한다.
6. 로그 size rotation, 저장소별 quota, disk cache 증분 index를 적용한다.
7. 코드 기반 설정 정본에서 포트·URL·권한 문서를 생성하거나 CI로 문서 예제를 검증한다.

---

## 10. 운영 체크리스트

### 배포 전 최소 통과 조건

- [ ] Raw Data 교체 API가 신뢰된 관리자 경로 밖에서 차단됨
- [ ] 업로드 body/rate/concurrency 제한이 실제 프록시 설정에 반영됨
- [ ] `.venv`에서 필수 모듈과 eval export smoke test가 성공함
- [ ] Honey 다운로드가 200이며 게시 hash와 실제 파일 hash가 일치함
- [ ] DB 외부 복제 백업이 있고 다른 디렉터리 복원·앱 기동을 완료함
- [ ] 서비스 자동 시작·graceful stop·crash restart가 확인됨
- [ ] HTTPS URL만 배포 클라이언트에 설정됨
- [ ] 디스크·RAM·5xx·DB lock 경보가 담당자에게 전달됨

### 매일/매주 확인

- 매일: free-space, DB backup 결과, 5xx, DB lock, worker/process/thread, 업로드 실패, 단일 로그 크기
- 매주: DB session과 로컬/S3 manifest 대조, 고아 관계 행, Honey 릴리스 smoke, cold 분석 응답, backup restore 표본
- 배포마다: 포트·바인딩·URL, requirements fingerprint, schema version, `/healthz`와 readiness, 다운로드 hash

---

## 11. 최종 판정

핵심 업로드·분석·편집·비공개·편집자 위임·삭제·탭 렌더링은 작은 격리 데이터에서 대체로 정상 작동했다. 그러나 **원본 무이력 덮어쓰기(P0)**, **무인증 대용량 업로드**, **저장소/DB 비원자성**, **현재 배포 환경의 필수 의존성 누락**, **깨진 Honey 배포물**, **동일 장애 영역 백업**, **강제 종료와 무감시 실행**이 함께 있어 현재 상태를 공개 운영 준비 완료로 볼 수 없다.

사용자가 수용한 기본 관리자 인증값과 Honey 식별 위조 가능성을 제외하더라도 결론은 동일하다. 먼저 즉시 조치의 외부 차단·의존성 검증·백업 분리·실행 감시를 적용한 뒤 제한된 내부 운영을 시작하고, 7일 조치가 끝난 후 공개 범위를 재평가하는 것이 안전하다.

---

## 12. 변경 보존 최종 확인

- 최종 `git status --porcelain=v1 -uall`에는 `PROJECT_OPERATION_UX_REVIEW.md` 한 파일만 새 파일로 표시됐다.
- 추적 중인 기존 파일의 `git diff --stat`, `git diff --check`는 비어 있었다.
- 운영 DB는 시작·종료 모두 864,256 bytes, 최종 수정 시각 `2026-07-15T12:13:25.0397217Z`였다.
- 운영 업로드는 시작·종료 모두 297개, 합계 301,890,554 bytes였다.
- DB 백업은 시작·종료 모두 7개, 합계 6,049,792 bytes였다.
- 서버 로그는 시작·종료 모두 30개, 합계 15,472 bytes였다.
- 최종 확인 시 5000, 8000, 8080 및 격리 검증 포트 8765, 8766에 listener가 없었다.

따라서 이번 검토가 남긴 작업 트리 변화는 이 Markdown 보고서 한 파일뿐이다.
